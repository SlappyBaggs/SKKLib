using Megahard.Collections;
using System;
using System.ComponentModel;
using System.Windows.Forms;

// Some common data binding stuff here
namespace Megahard.Data.Binding
{/*
	public class PropertyInfo
	{
		public PropertyInfo(object instance, PropertyPath prop)
		{
			System.Diagnostics.Debug.Assert(instance != null && prop != null);
			PropertyPath = prop;
			Instance = instance;
		}
		public PropertyPath PropertyPath { get; private set; }
		public object Instance { get; private set; }
		public PropertyDescriptor PropertyDescriptor
		{
			get
			{
				if (Instance == null || PropertyPath == null)
					return null;
				try
				{
					return PropertyPath.ResolveProperty(Instance);
				}
				catch (PropertyPath.FailedToResolvePropertyException)
				{
					return null;
				}
			}
		}
	}
  * */
}