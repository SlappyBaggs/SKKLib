using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Megahard
{
	public static class Parse
	{
		public static T Enum<T>(string val)
		{
			return (T)System.Enum.Parse(typeof(T), val);
		}
	}
}
